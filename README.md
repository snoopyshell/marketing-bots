# Marketing Bots

Public directory of Grok Bot marketing templates.

Static Next.js App Router site. Catalog lives in `data/packs.ts`. No CMS, database, or login.

```bash
npm install
npm run dev
npm run build
```
